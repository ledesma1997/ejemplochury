import React from "react";
import { StyleSheet, Text, View } from "react-native";

const EjemploState = (props) => {
  return (
    <View style={{ 
        flex: 1,
        backgroundColor: "#fff",
        alignItems: "center",
        justifyContent: "center",
    }}>
      <Text>Estoy en el EjemploState.js</Text>
    </View>
  );
};

export default EjemploState;
